# wasmspec — L18355: noCallFrameReturn APPROACH

## ABSOLUTE RULES
- **DO NOT** edit ClosureConvertCorrect.lean — jsspec owns it
- **DO NOT** run `lake build` — USE LSP ONLY.
- **DO NOT** use while/until loops, pgrep, sleep loops
- You CAN edit ANFConvertCorrect.lean AND Flat/Semantics.lean
- **CRASH PREVENTION**: KEEP TASKS SMALL.

## MEMORY: ~500MB free. USE LSP ONLY — no builds.

## STATUS — 2026-04-12T07:30
- Total: **42 real sorries** (ANF 30, CC 12). Unchanged from 06:05.
- EvalFirst approach for L18355: **DEAD** (your own analysis confirmed it).
- step_error_isLit: PROVED and has 0 sorries. It works. Don't doubt it.

## P0: L18355 — NEW APPROACH: noCallFrameReturn predicate (-1 sorry)

Your own analysis (L18365) identified approach (A) as viable:
> Thread noCallFrameReturn as precondition + prove ¬Head when all tryCatch are user-level

### The Plan:

**Step 1**: Define or find `NoTryCatchAnywhere : Flat.Expr → Prop` (recursive, checks ALL sub-expressions).
Alternatively, use existing `noCallFrameReturn` if it already implies no non-call-frame tryCatch.

**Step 2**: Prove: normalizeExpr in .return context produces expressions with no non-call-frame tryCatch anywhere.
Key facts you already proved:
- `normalizeExpr_tryCatch_not_return_none/some` — normalizeExpr never produces tryCatch in return context
- `normalizeExpr_return_implies_noEvalFirstTryCatch` — partial but insufficient

The insight: in a well-formed program with noCallFrameReturn, ALL tryCatches generated by normalizeExpr
are call-frame tryCatches (wrapping function calls). So `¬HasNonCallFrameTryCatchInHead` holds because
any tryCatch in head position IS a call-frame tryCatch.

**Step 3**: Prove `NoNonCallFrameTryCatchAnywhere e → ¬HasNonCallFrameTryCatchInHead e` (trivial).

**Step 4**: Prove preservation through steps: `NoNonCallFrameTryCatchAnywhere e → step? produces e' → NoNonCallFrameTryCatchAnywhere e'`.
This is where the previous approach failed (value transitions). But with "Anywhere" instead of "EvalFirst",
stepping `seq (.lit v) b` is fine because `NoNonCallFrameTryCatchAnywhere (seq a b)` implies it for both a AND b.

**Step 5**: Swap into L18355:
- HasReturnInHead_Steps_steppable gets `hncf : NoNonCallFrameTryCatchAnywhere a` as precondition
- Prove ¬HasNonCallFrameTryCatchInHead from hncf
- Update callers to provide hncf (should follow from normalizeExpr + noCallFrameReturn)

### WHY THIS WORKS (unlike EvalFirst):
- "Anywhere" is CLOSED under sub-expression extraction (value transitions preserve it)
- "EvalFirst" was NOT closed (only tracked first position, lost info on value transition)
- The cost: callers must prove the "Anywhere" property, but that follows from normalizeExpr analysis

## P1: BREAK/CONTINUE (L26931, L26932, L27151, L27222) — if P0 done

These need HasBreakInHead/HasContinueInHead infrastructure. Only attempt after P0.

## LOG
**FIRST**: `echo "### $(date -Iseconds) Starting run — [task]" >> agents/wasmspec/log.md`
**LAST**: `echo "### $(date -Iseconds) Run complete — [result]" >> agents/wasmspec/log.md`
